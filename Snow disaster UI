-- ==================================================
-- Snow Disaster Hub - 完整升级版 (UI 源码)
-- 支持：Tab 系统 + 外部加载按钮 + 注入器运行
-- ==================================================

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- 防止重复加载
if _G.SnowDisasterLoaded then return end
_G.SnowDisasterLoaded = true

-- ============================================
-- 1. UI 框架构建
-- ============================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SnowDisasterHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.Parent = playerGui

-- 主面板
local MainPanel = Instance.new("Frame")
MainPanel.Name = "MainPanel"
MainPanel.Size = UDim2.new(0, 480, 0, 320)
MainPanel.Position = UDim2.new(0.5, -240, 0.5, -160)
MainPanel.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainPanel.BackgroundTransparency = 0.15
MainPanel.Visible = false
MainPanel.Parent = ScreenGui

Instance.new("UICorner", MainPanel).CornerRadius = UDim.new(0, 14)

-- 标题
local Title = Instance.new("TextLabel", MainPanel)
Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundTransparency = 1
Title.Text = "Snow Disaster Hub"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 22
Title.TextColor3 = Color3.new(1, 1, 1)

-- ============================================
-- 2. Tab 系统 (核心升级)
-- ============================================
local Tabs = {}
local TabFrames = {}

local tabBar = Instance.new("Frame", MainPanel)
tabBar.Size = UDim2.new(1, 0, 0, 40)
tabBar.Position = UDim2.new(0, 0, 0, 50)
tabBar.BackgroundTransparency = 1

local tabLayout = Instance.new("UIListLayout", tabBar)
tabLayout.FillDirection = Enum.FillDirection.Horizontal
tabLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
tabLayout.Padding = UDim.new(0, 6)

-- 供外部调用的 API：创建 Tab
_G.CreateHubTab = function(name)
	if Tabs[name] then return TabFrames[name] end

	local tabBtn = Instance.new("TextButton", tabBar)
	tabBtn.Size = UDim2.new(0, 90, 1, 0)
	tabBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
	tabBtn.Text = name
	tabBtn.Font = Enum.Font.GothamBold
	tabBtn.TextSize = 14
	tabBtn.TextColor3 = Color3.new(1, 1, 1)
	tabBtn.AutoButtonColor = false
	Instance.new("UICorner", tabBtn).CornerRadius = UDim.new(0, 6)

	local frame = Instance.new("ScrollingFrame", MainPanel)
	frame.Size = UDim2.new(1, 0, 1, -90)
	frame.Position = UDim2.new(0, 0, 0, 90)
	frame.BackgroundTransparency = 1
	frame.ScrollBarThickness = 4
	frame.Visible = false
	Instance.new("UIListLayout", frame).Padding = UDim.new(0, 8)

	Tabs[name] = tabBtn
	TabFrames[name] = frame

	tabBtn.MouseButton1Click:Connect(function()
		for n, f in pairs(TabFrames) do
			f.Visible = (n == name)
			Tabs[n].BackgroundColor3 =
				(n == name) and Color3.fromRGB(70, 70, 70) or Color3.fromRGB(45, 45, 45)
		end
	end)

	if #Tabs == 1 then
		frame.Visible = true
		tabBtn.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
	end

	return frame
end

-- ============================================
-- 3. 按钮创建 API (供外部调用)
-- ============================================
_G.CreateHubButton = function(tabFrame, text, callback)
	local btn = Instance.new("TextButton", tabFrame)
	btn.Size = UDim2.new(0.95, 0, 0, 40)
	btn.BackgroundColor3 = Color3.fromRGB(55, 55, 55)
	btn.Text = text
	btn.Font = Enum.Font.Gotham
	btn.TextSize = 14
	btn.TextColor3 = Color3.new(1, 1, 1)
	btn.AutoButtonColor = false
	Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)

	btn.MouseButton1Click:Connect(function()
		if callback then callback() end
	end)
end

-- ============================================
-- 4. 悬浮窗 (UI 开关)
-- ============================================
local Float = Instance.new("ImageButton", ScreenGui)
Float.Size = UDim2.new(0, 44, 0, 44)
Float.Position = UDim2.new(0, 20, 0, 20)
Float.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
Float.Image = "rbxassetid://138791746843191" -- 你的头像 ID
Float.AutoButtonColor = false
Instance.new("UICorner", Float).CornerRadius = UDim.new(1, 0)

Float.MouseButton1Click:Connect(function()
	MainPanel.Visible = not MainPanel.Visible
end)
